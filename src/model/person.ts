export type Person = {
    id: number;
    kürzel: string;
    title : string;
    email : string;
}

export type Submission = {
    listId: string,
    id: string,
    name: string,
    item: string,
    people: string,
}